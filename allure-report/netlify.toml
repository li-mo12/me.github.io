# netlify.toml 示例
[build]
  command = "echo 'No build needed for static HTML'"  # 无构建步骤可留空
  publish = "allure-report"  # 指向 Allure 报告目录

# 处理 404 路由 (SPA 支持)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200